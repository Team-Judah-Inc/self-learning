apiVersion: v1
kind: Service
metadata:
  name: {{ include "selflearningchart.fullname" . }}
  labels:
    {{- include "selflearningchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "selflearningchart.selectorLabels" . | nindent 4 }}
